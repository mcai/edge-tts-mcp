# Core dependencies
edge-tts>=6.1.5
fastmcp>=0.2.0
pydantic>=2.0.0  # Require Pydantic v2 for field_validator

# API server dependencies (for SSE transport)
fastapi>=0.95.0
uvicorn>=0.22.0

# Note: asyncio is part of the Python standard library and doesn't need to be in requirements.txt
