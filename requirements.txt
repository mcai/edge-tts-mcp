# Core dependencies
edge-tts==7.0.0  # Latest version as of April 2025
fastmcp==2.0.0   # Latest version as of April 2025
pydantic==2.11.3 # Latest version as of April 2025

# API server dependencies (for SSE transport)
fastapi==0.115.12
uvicorn==0.34.0

# Note: asyncio is part of the Python standard library and doesn't need to be in requirements.txt
